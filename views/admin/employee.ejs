<%-include("../common/header")%>

    <link rel="stylesheet" href="/admin/hospital.css">
    <title>Employee : Blood Bank</title>
</head>
<body>
    <%-include("navbar")%>

    <h1> <%= employee.name %> </h1>

    <section class="main">
        <div class="side-bar">
            <table class="side-table">
                <thead><tr><th>Admin</th></tr></thead>
                <tbody>
                    <tr><td><a href= <%= "/admin/"+adminId+"/donors" %> >Donors</a></td></tr>
                    <tr><td><a href=<%= "/admin/"+adminId+"/employees" %>>Employee</a></td></tr>
                    <tr><td><a href=<%= "/admin/"+adminId+"/hospitals" %>>Hospital</a></td></tr>
                    <tr><td><a href=<%= "/admin/"+adminId+"/bloodRequests" %>>Blood Requests</a></td></tr>
                    <tr><td><a href=<%= "/admin/"+adminId+"/blood" %>>Blood Inventory</a></td></tr>
                </tbody>
                <tfoot><tr><td></td></tr></tfoot>
            </table>
        </div>
        <div class="main-item">
            <table class="main-table">
                <tr>
                    <td class="left-col">Name : </td>
                    <td class="right-col"> <%= employee.name %> </td>
                </tr>
                <tr>
                    <td class="left-col">Gender : </td>
                    <td class="right-col"> <%= employee.gender %> </td>
                </tr>
                <tr>
                    <td class="left-col">Aadhar Number : </td>
                    <td class="right-col"> <%= employee.aadhar %> </td>
                </tr>
                <tr>
                    <td class="left-col">e-mail : </td>
                    <td class="right-col"> <%= employee.email %> </td>
                </tr>
                <tr>
                    <td class="left-col">Mobile No. : </td>
                    <td class="right-col"> <%= employee.mobile %> </td>
                </tr>
                <tr>
                    <td class="left-col">Date of Birth : </td>
                    <td class="right-col"> <%= employee.dob.getDate()+"/"+(employee.dob.getMonth()+1)+"/"+employee.dob.getFullYear() %> </td>
                </tr>
                <tr>
                    <td class="left-col">Job Title : </td>
                    <td class="right-col"> <%= employee.jobTitle %> </td>
                </tr>
                <tr>
                    <td class="left-col">Address : </td>
                    <td class="right-col"> <%= employee.address %> </td>
                </tr>
                <tr>
                    <td class="left-col">Date of Joining : </td>
                    <td class="right-col"> <%= employee.dateOfJoining.getDate()+"/"+(employee.dateOfJoining.getMonth()+1)+"/"+employee.dateOfJoining.getFullYear() %> </td>
                </tr>
                <tr>
                    <td class="left-col"><a href= <%= "/admin/"+adminId+"/employees/"+employee._id+"/edit" %> ><button>Edit</button></a></td>
                    <td class="right-col"><a href= <%= "/admin/"+adminId+"/employees/"+employee._id+"/delete" %> ><button onclick="return recheck();">Delete</button></a></td>
                </tr>
            </table>
        </div>
    </section>
    
    <%-include("../common/footer")%>

    <script>
        function recheck(){
            if(confirm("Are you sure to delete the EMPLOYEE ? ")===true){
                return true;
            }
            else{
                return false;
            }
        }
    </script>
</body>
</html>